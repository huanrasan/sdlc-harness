# SDLC harness (extension)

This extension adds SDLC skills. They apply only in repositories that contain `harness.toml`; follow that
repository's `AGENTS.md`. If the user wants the harness in a repository without `harness.toml`, use the
`sdlc-install` skill and confirm before writing files. Never approve artifacts on behalf of a human.
